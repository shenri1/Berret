# npm completions
if command -v npm &>/dev/null; then
  eval "$(npm completion)"
fi

# docker completions (via Oh My Zsh plugin, but fallback)
if command -v docker &>/dev/null; then
  if [[ -f /usr/share/zsh/site-functions/_docker ]]; then
    autoload -Uz _docker
  fi
fi
